# Dependencias
node_modules/
.pnpm-store/

# Build
dist/
build/
.next/
out/
*.tsbuildinfo

# Entorno — NUNCA subir secretos
.env
.env.*
!.env.example
# Plantilla del frontend: es un ejemplo sin secretos y tiene que viajar en el repo.
!.env.local.example

# Llaves
*.pem
*.key
secrets/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Tests / cobertura
coverage/
.vitest/
test-results/
playwright-report/
.ai-agent-eval-results*.jsonl
# Sesión guardada por los E2E: lleva un token de verdad.
# Sin barra inicial y con `**`, porque el patrón `e2e/.auth/` queda anclado a
# la raíz del repo y no cubría `apps/web/e2e/.auth/`.
**/e2e/.auth/

# Editor / SO
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Datos locales
*.sqlite
*.sqlite3
data/

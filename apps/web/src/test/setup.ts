import '@testing-library/jest-dom/vitest';

/**
 * Matchers de `jest-dom` (`toBeDisabled`, `toBeInTheDocument`…) para todos los
 * tests. En los que corren en Node no estorban: solo añaden aserciones.
 */

node_modules
dist
.next
coverage
pnpm-lock.yaml

# Generados: los produce una herramienta y la CI comprueba que están al día.
# Si Prettier los reformatea, deja de coincidir con lo que genera el script.
docs/api/openapi.json
apps/api/drizzle/

# Lo regenera `next dev` en cada arranque; formatearlo solo crea ruido en el diff.
apps/web/next-env.d.ts

# Artefactos de los E2E: los genera Playwright, no se revisan ni se suben.
apps/web/e2e/.auth/
apps/web/test-results/
apps/web/playwright-report/
